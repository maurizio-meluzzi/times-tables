server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Disable caching for local development — forces browser to always fetch fresh files
    add_header Cache-Control "no-store";

    location / {
        try_files $uri $uri/ /index.html;
    }
}
